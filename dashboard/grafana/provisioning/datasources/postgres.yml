apiVersion: 1
datasources:
  - uid: timescaledb
    name: TimescaleDB
    type: postgres
    url: postgres:5432
    database: crypto_pipeline
    user: pipeline
    secureJsonData:
      password: "${GF_DATASOURCE_POSTGRES_PASSWORD}"
    jsonData:
      sslmode: disable
      timescaledb: true
    isDefault: true
